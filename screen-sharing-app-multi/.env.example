# Server Configuration
PORT=3000
NODE_ENV=production

# Security
# Generate a strong secret using: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here

# Database
# Optional: MongoDB Connection String (if using a cloud database)
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/airsign
